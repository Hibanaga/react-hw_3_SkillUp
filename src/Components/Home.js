import React from "react";

const Home = () => {
  return (
    <div>
      <h1>Welcome to start page</h1>

      <span>
        all tasks work in react router with usage functionla components with
        hocks{" "}
      </span>
    </div>
  );
};

export default Home;
